"""Persistence adapters for the core's SessionStore port. Lives outside
core/ for the same reason the LLM providers do: the hexagon stays
stdlib+pydantic only (enforced by tests/test_boundary.py)."""
